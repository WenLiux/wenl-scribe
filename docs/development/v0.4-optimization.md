# 留文 v0.4 优化实施记录

更新日期：2026-07-21

## 本轮结果

本轮围绕“总结必须忠于原视频”和“长任务失败后可恢复”完成了第一轮工程化改造。新任务会保留带时间戳的转录分段，AI 输出的每条观点必须绑定可在逐字稿中定位的原文证据；无法定位的观点不会进入最终总结。

## 已完成

### 1. 时间戳与证据链

- 公开字幕和本地 Whisper 转录统一保存为 `start / end / text / source` 分段。
- 总结观点统一为 `claim / evidence / kind / start / end / context`。
- AI 原文引句必须能在逐字稿中精确定位，支持跨相邻分段匹配。
- 结果页可查看“观点—原文—时间点—上下文”，并从时间点跳回原视频。
- 逐字稿页面按时间段展示；转录与总结 Markdown 均带时间信息。

### 2. 总结可靠性

- 强制结构化 JSON 输出，并校验字段、观点类型和原文证据。
- 长逐字稿按分段处理，单个分块失败不会清空其他已验证结果。
- API 失败时保留逐字稿并生成本地原文摘录，任务标记为“部分完成”，不再伪装成完整 AI 总结。
- 支持只重新总结，无需重复下载和转录。

### 3. 任务恢复与数据安全

- 新增等待、处理中、已完成、部分完成、失败、已取消等统一状态。
- 支持取消任务、从已保存阶段重试、强制重新转录。
- 每个任务使用独立目录 `data/tasks/<job_id>/`。
- 分阶段原子写入元数据、状态、逐字稿、总结、Markdown 和脱敏日志。
- 转录完成后立即落盘，即使总结失败也能下载逐字稿。
- 旧版平面任务自动迁移，旧文件暂时保留作为兼容备份。

### 4. 进度、历史与诊断

- 下载显示实际字节进度，转录显示已处理音频时长，总结显示分块进度。
- 历史页支持按标题、作者、BV 号搜索和按状态筛选。
- 失败页显示结构化错误码、失败阶段和是否可重试。
- 新增脱敏诊断 Markdown 下载接口，旧任务没有日志时也会输出当前任务快照。

### 5. 界面与隐私

- 使用新版 WENL Logo 视觉。
- 补齐观点证据卡、时间戳逐字稿、部分成功提醒和恢复操作样式。
- 修复移动端底部导航被页头模糊效果限制而发生重叠的问题。
- 设置页明确云端边界：音频和 Whisper 转录留在本机，主动使用云端总结时只发送逐字稿。
- API Key 不通过配置接口返回；诊断日志会脱敏。

## 接口

```text
POST /api/jobs
GET  /api/jobs/<job_id>
POST /api/jobs/<job_id>/cancel
POST /api/jobs/<job_id>/retry
POST /api/jobs/<job_id>/resummarize
GET  /api/history
GET  /api/download/transcript?job_id=<job_id>
GET  /api/download/summary?job_id=<job_id>
GET  /api/download/diagnostics?job_id=<job_id>
```

## 验证结果

- Python 编译通过。
- 后端单元测试：3 项通过。
- 前端生产构建通过。
- 页面与接口契约测试：3 项通过。
- ESLint：0 错误，2 个图片性能提示。
- 本地运行验收：前端 `http://localhost:3001/` 返回 200，后端健康检查通过。
- 三个 Markdown 下载接口均返回 200，诊断内容未检测到密钥特征。
- 浏览器检查首页、历史迁移、旧任务兼容、设置隐私说明和移动端布局；控制台无错误。

## 已知边界

- 旧任务原始数据没有时间戳，只能显示“旧任务无时间戳”；新任务才具备完整时间证据链。
- 取消是协作式取消：下载和逐段转录可及时停止，模型初始化期间可能需要等待当前步骤结束。
- API Key 当前以明文保存在本机 `data/config.json`，不要提交或分享该文件。
- 本轮没有调用外部 Gemini 测试接口，避免继续使用此前曾出现在截图中的密钥；建议先在 Google AI Studio 撤销旧密钥并生成新密钥。

## 下一步建议

1. 换新 Gemini API Key 后，用一个 5–10 分钟视频做完整回归，重点检查时间跳转和观点原文一致性。
2. 收集 10 个不同类型视频，建立“转录准确率、证据命中率、总结忠实度”的固定评测集。
3. 第二轮再考虑在线转录供应商适配、队列并发限制和桌面安装包。
