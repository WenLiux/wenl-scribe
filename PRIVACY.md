# 留文隐私说明

最后更新：2026-07-24

留文是一款本地优先工具。本说明描述当前 Windows 便携版和源码运行版如何处理数据。

## 本机处理的数据

留文会在本机保存：

- 用户粘贴的视频链接和公开元数据；
- 获取到的公开字幕或本地 Whisper 生成的逐字稿；
- 内容总结、原文依据和 Markdown 文件；
- 任务状态、设置和脱敏诊断日志；
- 下载的 Whisper 模型；
- 用户主动填写的总结服务 API Key。

Windows 便携版默认保存在 `%LOCALAPPDATA%\WENL Scribe\data`，源码开发模式默认保存在项目 `data/`。

## 会访问的外部服务

为了完成用户主动创建的任务，留文可能访问：

- 哔哩哔哩：解析链接、读取公开视频信息、字幕、封面、音频地址和官方播放器；
- Hugging Face：首次使用某个 Whisper 模型时下载模型文件；
- 用户选择的总结服务：Gemini、SenseNova、OpenAI 或其他兼容接口。

没有公开字幕时，音频由本机 Whisper 转录。启用云端总结后，带时间戳逐字稿会发送给用户选择的总结服务；音频不会作为总结请求上传。

外部服务如何处理数据，由其各自的隐私政策和服务条款决定。

## API Key

API Key 以明文保存在本机配置文件中，完整密钥不会通过留文配置接口返回给浏览器。请注意：

- 不要提交或分享 `data/config.json`；
- 不要把密钥粘贴到 Issue、诊断文件、截图或聊天记录中；
- 如果密钥曾经公开出现，应立即在服务商控制台撤销并更换；
- 公共或多人共用电脑不适合保存个人 API Key。

## 遥测与账户

当前版本：

- 不提供留文账户系统；
- 不包含留文自建的数据收集服务器；
- 不主动上传使用统计或遥测数据；
- 不会自动把任务历史同步到云端。

第三方播放器、模型下载和总结 API 仍可能按照各自规则记录网络请求。

## 删除数据

用户可以在留文历史记录中删除单个任务。若要彻底清除本机数据，可退出留文后删除对应数据目录。删除前请先备份需要保留的 Markdown 文件。

## 内容责任

用户应确保自己有权访问和处理相关视频。请勿使用留文规避付费、权限、地区或版权限制。

## 变更

如果未来增加在线服务、账户、遥测或云端存储，本说明会在功能发布前更新。
