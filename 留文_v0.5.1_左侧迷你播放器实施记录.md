# 留文 v0.5.1 左侧迷你播放器实施记录

日期：2026-07-23

## 完成内容

- 首页品牌标语统一为“所见所听，皆可留文”，集中维护于 `app/brand.ts`。
- 页面描述同步使用新标语，源文件中不再保留旧文案。
- 结果页始终只渲染一个 `BilibiliPlayer` 和一个 B 站 iframe。
- 原位播放器完全离开视口上方后，桌面端自动固定为左下迷你播放器。
- 滚回原位后自动恢复普通播放器，浮窗切换本身不修改 iframe 的 `src` 或 `key`。
- 使用 `ResizeObserver` 保存原位高度，播放器脱离文档流时正文不跳动。
- 桌面浮窗支持“返回原位”“关闭”和“显示视频”恢复。
- 关闭后外壳保持脱离文档流但不可见，避免浏览器滚动锚定把用户带回页面上方。
- 已关闭的桌面浮窗在用户点击时间戳时会重新显示。
- 移动端默认显示底部悬浮条，用户展开或点击时间戳后显示完整播放器。
- 移动端“收起”回到悬浮条，不长期遮挡总结正文。
- 桌面和移动端展开状态使用视频下方独立控制条，定位时间及操作按钮不再覆盖画面。
- 时间戳点击增加 150ms 合并，快速连续点击时只执行最后一次定位。
- 切换任务或卸载页面时清理待执行定位定时器；观察器在组件卸载时断开。
- 保留自动播放失败及 iframe 不可用时的 B 站站外入口。

## 关键实现

- `useFloatingPlayer.ts`：观察原位容器，只在其离开视口上方后进入浮窗状态。
- `FloatingBilibiliPlayer.tsx`：保持播放器 DOM 不变，只切换外壳 CSS。
- `useMediaQuery.ts`：响应桌面与移动端切换，不因断点变化卸载 iframe。
- `page.tsx`：统一时间戳处理、唤回浮窗和 150ms 连点合并。
- `globals.css`：桌面左下浮窗、移动端悬浮条、安全区、层级与减少动态效果。

## 当前项目适配

当前留文没有固定左侧导航，因此 `--app-left-sidebar-width` 默认为 `0px`；后续增加侧栏时只需由应用外壳更新该变量。移动端现有底部导航高度为 58px，播放器使用 `--app-bottom-safe-area` 自动避让。

当前页面没有模态框体系，因此本版没有额外增加模态框抑制状态；浮窗层级为 60，后续模态框应使用 100 以上层级。

## 验证范围

- 首页新标语、页面描述和旧文案全局搜索。
- 单 iframe 源码契约。
- IntersectionObserver 与 ResizeObserver 接入。
- 桌面滚动浮出、返回原位、关闭、恢复和时间戳唤回。
- 移动端折叠条、展开、收起和时间戳自动展开。
- 同一时间戳重复定位与快速连续点击最终位置。
- 构建、自动化测试、代码检查和 CSP 响应头。
