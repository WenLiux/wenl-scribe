# 版本记录

本项目使用 `主版本.次版本.修订版本` 的版本号形式。公开发布前的版本可能包含不兼容调整。

## [Unreleased]

## [0.7.3] - 2026-08-06

### 新增

- 重写总结 API 设置流程：先选服务商、再输入 API Key；SenseNova 支持一键读取模型并自动选择。

### 改进

- 增加按能力进行模型发现：OpenAI、Gemini 官方兼容接口和标准兼容服务读取 `/models`，Ollama 读取 `/api/tags`；不提供列表接口的私有服务保留默认/手动模型入口。
- 将协议、API 地址和模型 ID 收进“高级设置”，减少服务商、协议和模型选项互相污染。
- API 接入增加显式协议层，区分供应商、协议和模型；旧配置会在读取时兼容迁移。
- 新增 `backend/llm` Adapter、统一响应元数据和 HTTP 错误分类，避免仅通过 URL 后缀推断 SenseNova 请求格式。
- “测试连接”改为轻量连通性探测，不再要求返回完整的总结观点或原文证据。
- 任务和诊断信息记录实际总结供应商、协议和模型，便于复现和排查。
- Windows 优先使用当前用户 DPAPI 加密保存新 API 密钥；保留旧版明文配置读取，并在主动保存时迁移。
- 本地后端不再对所有来源开放 CORS；开发端口可通过 `WENL_ALLOWED_ORIGINS` 显式配置。
- 同时兼容 SenseNova 官方 `/llm/chat-completions` 与旧版 OpenAI 兼容 `/chat/completions` 路径。
- 兼容 SenseNova `data.choices` 响应封装、字符串消息和文本块消息格式。
- 官方 SenseNova 请求使用 `max_new_tokens` 与文本内容块，并避免发送可能被拒绝的 `response_format` 扩展。
- 总结响应解析改为按通用响应结构递归提取，不再按具体模型名称编写解析分支。
- 将 HTTP 403 与 API Key 无效区分开；商汤错误码 7 会提示检查订阅中心的接口/模型权限。

## [0.7.2] - 2026-08-04

### 改进

- 为 Windows 便携版指定留文专属应用图标，替换 PyInstaller 默认的 Python 图标。
- 构建脚本会在打包前自动生成多尺寸 `wenl.ico`，确保图标与品牌 Logo 保持一致。

## [0.7.1] - 2026-08-04

### 改进

- 统一首页、任务页、总结页、历史页和悬浮播放器的箭头、播放、关闭、勾选与警告图标，改善小尺寸显示和无障碍语义。
- 使用新版 `wenl_logo2.svg` 作为应用品牌标识，优化浏览器 favicon，并为 Windows 便携版加入留文专属 `.ico` 应用图标。
- 清理未使用的 Next.js 模板图标资源，补充品牌资产与许可说明。

## [0.7.0] - 2026-07-26

### 新增

- 新建转录前增加模型确认，首次使用默认选择 Whisper Small。
- 首次开始转录时增加内容权限、结果核对、本机资源与云端隐私确认；总结 API 可选择“以后再说”。
- 悬浮播放器离开原位后默认弹出，主动缩略后变为固定尺寸的可拖动按钮，并从按钮附近自适应展开。
- 内容结果页和设置页增加全局“默认弹出悬浮窗”开关，并记住新任务的首次下滑偏好。
- 采用 PolyForm Noncommercial License 1.0.0，明确源码公开但仅限非商业使用。
- 增加商业使用、用户输出归属、品牌权利、第三方软件和 Required Notice 说明。
- 增加贡献者许可协议，保留公开非商业版本与单独商业授权的权利链。
- Windows 便携版构建时自动附带许可证和相关权利文件。

### 改进

- 修复核心观点“定位播放”可能跳到前一段内容的问题，并提升段内定位精度。
- 内容总结仍以左下角、完整转录仍以右下角作为默认悬浮播放器区域。
- 内容总结页的默认悬浮窗缩略后，按钮贴靠页面左下角；短总结滚到底时也能触发悬浮播放器。
- 当前任务再次下滑时恢复上滑前的播放器状态：上滑前展开则再次展开，上滑前缩略则再次显示按钮。
- 切换“默认弹出悬浮窗”会同步更新当前任务的下一次下滑状态；关闭后先显示缩略按钮，之后仍可主动展开并记住新状态。
- 整理仓库文件结构，将使用文档、许可说明、发布资料和开发记录归入分组目录。
- 增加统一的文档导航页，减少 GitHub 根目录中的零散文件。

## [0.6.1] - 2026-07-24

### 新增

- 内容总结页悬浮播放器默认位于左侧。
- 完整转录页悬浮播放器默认位于右侧。
- 两个页面均可将悬浮播放器缩略为同一位置的“视频”按钮。
- Markdown 导出增加视频信息表、可点击时间戳、原文引用、内容脉络和生成说明。
- 导出文件按“视频标题＋总结／逐字稿＋留文”命名。

### 改进

- 中文 Markdown 下载文件名兼容主流浏览器。
- 自动清理 Windows 文件名不支持的字符。

## [0.6.0] - 2026-07-24

### 新增

- Windows 便携内测版构建流程。
- Python、Whisper 运行库和静态前端统一封装。
- 系统托盘入口，支持打开和退出留文。
- 便携版数据迁移到 `%LOCALAPPDATA%\WENL Scribe\data`。

### 验证

- Windows 便携程序能够独立启动本地服务和前端页面。
- Node 与 Python 自动化测试通过。
