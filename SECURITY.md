# 安全政策

## 支持范围

当前仅维护最新发布的 Windows 内测版本和默认分支。较早的测试包可能不会获得安全修复。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告。公开仓库创建后，应先开启 Private vulnerability reporting。

报告内容建议包括：

- 受影响版本；
- 问题类型和潜在影响；
- 可重复的最小步骤；
- 必要的脱敏日志；
- 建议的修复方向（如有）。

请不要在公开 Issue 中披露可利用细节，也不要提交真实 API Key、访问令牌、个人信息、完整任务数据或未公开视频内容。

## 密钥泄露

如果 API Key 已经出现在截图、日志、提交记录或公开讨论中：

1. 立即前往对应服务商控制台撤销旧密钥；
2. 创建新密钥；
3. 清理本机配置和公开材料中的旧值；
4. 不要仅通过删除 Git 提交来假设旧密钥已经安全。

## 安全边界

留文绑定在本机回环地址，不应直接暴露到公网。不要修改为监听所有网络接口，除非已经增加身份验证、访问控制、请求限制和安全审计。
